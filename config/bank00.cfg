bank = 0
auto_vectors

# Accepted from repeatable Mesen evidence. The observed exact entry
# variant is $00:89C9 M1X0 and returns M1X0 through the continuation trampoline
# at $00:C49D.
exit_mx_at 0089c9 1 0

# $008B selects one of 32 interleaved 16-bit handler addresses at $00:C1AE.
# The table ends at $00:C1ED, immediately before its first handler.
indirect_dispatch c1ab 32 idx:X tables:c1ae

# This table becomes an AOT boundary only when the rejected $07:AC47
# coroutine experiment is enabled. Keep it inactive until that contract is
# proven independently.
# indirect_dispatch d474 32 idx:Y tables:d477

# Exact native entries observed in tier-2 gameplay and repeated bounded Mesen
# captures, then accepted through replay comparisons and gameplay testing.
# No callee exit-width contract is asserted. Normal generation selects these
# roots for instruction timing; see docs/AOT_COVERAGE.md.
func bank_00_C3DE C3DE entry_mx:1,0
func bank_00_C7A0 C7A0 entry_mx:1,0
